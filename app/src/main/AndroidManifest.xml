<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dpene.database">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GameOverActivity" />
        <activity android:name=".LetterActivity" />
        <activity android:name=".LoadingActivity" />
        <activity android:name=".LogicalQuestionActivity" />
        <activity android:name=".MapActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".RegularQuestionActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".SaveLifeActivity" />
        <activity android:name=".RebornActivity"></activity>
    </application>

</manifest>
